// import { enc } from 'crypto-js';
// import AES from 'crypto-js/aes';

// export function aesEncrypt(key: string, data: string): string {
//   const ciphertext = AES.encrypt(JSON.stringify({ data }), key);
//   const res = ciphertext.toString();
//   return res;
// }

// export function aesDecrypt(key: string, data: string): string {
//   return AES.decrypt(enc.Base64.parse(data).toString(enc.Utf8), key).toString(enc.Utf8);
// }
